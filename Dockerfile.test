FROM python:3.11-slim

WORKDIR /app

COPY requirements-test.txt .
RUN pip install --no-cache-dir -r requirements-test.txt

# No COPY of source files — they are mounted at runtime via -v
# This means the same image works for both code quality and test stages

CMD ["pytest", "tests/", "-v"]