[pytest]
minversion = 7.0
addopts =
    --tb=short
    --strict-markers
    -q
testpaths = tests
markers =
    smoke: fast sanity checks
    functional: full integration tests
    contract: schema + SLA assertions